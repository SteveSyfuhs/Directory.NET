apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: directory-net

commonLabels:
  app.kubernetes.io/part-of: directory-net

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - server-statefulset.yaml
  - server-service.yaml
  - web-deployment.yaml
  - web-service.yaml
  # Uncomment to enable ingress:
  # - web-ingress.yaml
